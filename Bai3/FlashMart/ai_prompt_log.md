Prompt 1:  Cách sử dụng hàm COUNT() kết hợp với GROUP BY khi có sử dụng LEFT JOIN.

Prompt 2: Trong cơ sở dữ liệu MySQL, mặc định từ khóa JOIN (khi không ghi rõ LEFT hay RIGHT) sẽ hoạt động như thế nào? Nó sẽ bỏ qua hay giữ lại các bản ghi không có sự trùng khớp ở cả hai bảng?